[tool.poetry]
name = "toshokan-platform"
version = "0.1.0"
description = "Cross-cutting infrastructure primitives (storage, helpers)"
authors = []
packages = [{ include = "toshokan_platform" }]

[tool.poetry.dependencies]
python = ">=3.12,<4.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
